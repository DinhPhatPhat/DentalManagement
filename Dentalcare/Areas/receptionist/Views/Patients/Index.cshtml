@model IEnumerable<Dentalcare.Areas.admin.Models.CreatePatientViewModel>
@using Dental.Help
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/receptionist/Views/Shared/MyLayout.cshtml";
}

<!-- HTML -->
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Danh sách bệnh nhân</h6>
    </div>
    <div style="margin-left: 20px; margin-top: 20px">
        <button type="button" class="btn btn-info" onclick="window.location='@Url.Action("Create", "Patients")'">Thêm mới</button>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered" id="myTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>Thứ tự</th>
                        <th>Mã</th>
                        <th>Ảnh</th>
                        <th>Tên</th>
                        <th>Hoạt động</th>
                        <th>Meta</th>
                        <th>Ngày đăng ký khám</th>
                        <th>Tên tài khoản</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.patient.order)</td>
                            <td>@Html.DisplayFor(modelItem => item.account.id)</td>
                            <td>
                                @if (!string.IsNullOrEmpty(item.person.img))
                                {
                                    <img class="doctor-avatar" src="@item.person.img" alt="Avatar" style="max-width: 100px" />
                                }
                                else
                                {
                                    <img class="doctor-avatar" src="~/Content/images/default-avatar.png" alt="Default Avatar" style="max-width: 100px" />
                                }
                            </td>
                            <td>@Html.DisplayFor(modelItem => item.person.name)</td>
                            <td>@Html.DisplayFor(modelItem => item.account.able)</td>
                            <td>@Html.DisplayFor(modelItem => item.patient.meta)</td>
                            <td>@Html.DisplayFor(modelItem => item.patient.datebegin)</td>
                            <td>@Html.DisplayFor(modelItem => item.account.username)</td>
                            <td>
                                @Html.NoEncodeActionLink("<span class='glyphicon glyphicon-pencil'></span>", "Edit", "Edit", "Patients", routeValues: new { id = item.account.id }, htmlAttributes: new { data_modal = "", @class = "btn btn-default" })
                                @Html.NoEncodeActionLink("<span class='glyphicon glyphicon-zoom-in'></span>", "Detail", "Details", "Patients", routeValues: new { id = item.account.id }, htmlAttributes: new { data_modal = "", @class = "btn btn-primary" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
@section Scripts {
    <link href="@Url.Content("~/Areas/admin/Content/vendor/datatables/dataTables.bootstrap4.min.css")" rel="stylesheet">
    <!-- Bootstrap core JavaScript-->
    <!-- Page level plugins -->
    <script src="@Url.Content("~/Areas/admin/Content/vendor/datatables/jquery.dataTables.min.js")"></script>
    <script src="@Url.Content("~/Areas/admin/Content/vendor/datatables/dataTables.bootstrap4.min.js")"></script>

    <!-- Page level custom scripts -->
    <script src="@Url.Content("~/Areas/admin/Content/js/demo/datatables-demo.js")"></script>
}
